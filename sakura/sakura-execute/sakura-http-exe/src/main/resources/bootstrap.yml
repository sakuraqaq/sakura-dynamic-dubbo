dubbo:
  registry:
    address: spring-cloud://127.0.0.1
  scan:
    base-packages: com.sakura.controller
  protocol:
    name: dubbo
    port: -1
  cloud:
    subscribed-services: dubbo-provider


spring:
  application:
    name: dubbo-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:4396

